package com.raju.getmyshow.eventcatalog.domain.enums;

public enum ScreenType {
    STANDARD,
    IMAX,
    DOLBY,
    FOUR_DX,  // Can't use 4DX (can't start with number)
    THREE_D,  // Can't use 3D
    STADIUM_SECTION
}